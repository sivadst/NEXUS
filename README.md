# NEXUS
NEXUS is a graph-based disaster evacuation system that computes the safest routes in real time.  It dynamically updates path weights using crowd density and hazard levels.  The system uses Dijkstra’s algorithm with a priority queue for efficient routing.  It demonstrates how data structures enable intelligent decision-making in critical situations.
